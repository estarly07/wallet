<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Wallet" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark">@color/backgroundScreens</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.Wallet" parent="Base.Theme.Wallet" />
    <style name="buttonKeyboard" parent="Base.Theme.Wallet">
        <item name="android:background">@drawable/wallet_keyboard_button</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/key_keyboard</item>
    </style>
</resources>